import { Header } from "../../../components/Header";
import { Container } from './styles'


interface PersonalDataProps{
    step: number
}

export const PersonalData = ({step} : PersonalDataProps) => {
    return (
        <Container id="personalData">
        </Container>
    )
}